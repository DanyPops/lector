[dependencies]
internal-secret-crate = { version = "1.0.0", registry = "internal" }

# CARGO_REGISTRIES_INTERNAL_TOKEN redacted -- private registry credentials
# must never appear in a committed fixture or provenance record.
