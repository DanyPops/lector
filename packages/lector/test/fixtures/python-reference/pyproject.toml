[project]
name = "python-reference"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["requests==2.31.0"]

[tool.pyright]
include = ["contracts", "app", "legacy", "nspkg"]
pythonVersion = "3.11"
